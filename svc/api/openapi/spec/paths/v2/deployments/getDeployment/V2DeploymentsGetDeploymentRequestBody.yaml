type: object
required:
  - deploymentId
properties:
  deploymentId:
    type: string
    minLength: 1
    maxLength: 256
    description: |
      The unique identifier of the deployment to retrieve.
    example: dep_1234abcd
additionalProperties: false
description: Retrieve a single deployment by its id.
